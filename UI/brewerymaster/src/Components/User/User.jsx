import React, { useState } from "react";

import { useTranslation } from "react-i18next";

const User = () => {
  const { t } = useTranslation();

  return (
    <div>
<h2>User</h2>
    </div>
  );
};

export default User;
